[project]
name = "tailor_resume"
version = "0.1.0"
dependencies = [
    "python-docx>=1.2.0",
]

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
package-dir = {"" = "src"}
